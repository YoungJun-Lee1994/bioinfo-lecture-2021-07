"""
d_dict = {}
c = input("Enter three-base codon to build : ")

while c == "XXX":
    a = input("Enter amino acid: ")
    d_ca = {c: a}
    d_dict += d_ca
    continue
"""

# key=c : value = a
"""
강사님
반복문 for랑 while있는데 
for은 몇바퀴 돌 지 한정적인 반복일 때 쓴다
while은 언제 끝날지 모르는 상황에서 쓴다.
반복문을 제어할 수 있는 친구들은 continue, break

이거 풀이 사진 찍어놨다. 녹음도 여기서 다시 시작함. 210708 오전 52분 49초부터


"""
